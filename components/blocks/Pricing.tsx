import type { CmsBlock, RepeaterItem } from '@/types/cms';
export function Pricing({ block }: { block: CmsBlock }) { const plans = Array.isArray(block.plans) ? block.plans as RepeaterItem[] : []; return <section className="block muted"><div className="shell"><h2>{String(block.headline || '')}</h2><div className="cards cols-3">{plans.map((plan, index) => <article className={`card price ${plan.highlighted ? 'highlighted' : ''}`} key={index}><h3>{String(plan.name || '')}</h3><p className="amount">{String(plan.price || '')}<small>{String(plan.period || '')}</small></p><ul>{(Array.isArray(plan.features) ? plan.features : String(plan.features || '').split('\n')).map((feature, i) => <li key={i}>{String(feature)}</li>)}</ul>{plan.cta ? <a className="button" href="#contact">{String(plan.cta)}</a> : null}</article>)}</div></div></section>; }
