import type { CmsBlock, RepeaterItem } from '@/types/cms';
export function Testimonials({ block }: { block: CmsBlock }) { const items = Array.isArray(block.items) ? block.items as RepeaterItem[] : []; return <section className="block"><div className="shell"><h2>{String(block.headline || '')}</h2><div className="cards cols-3">{items.map((item, index) => <figure className="card quote" key={index}><div aria-label={`${Number(item.rating || 5)} iš 5`}>{'★'.repeat(Number(item.rating || 5))}</div><blockquote>„{String(item.quote || '')}“</blockquote><figcaption><strong>{String(item.author || '')}</strong>{item.role ? ` — ${String(item.role)}` : ''}</figcaption></figure>)}</div></div></section>; }
