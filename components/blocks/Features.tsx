import type { CmsBlock, RepeaterItem } from '@/types/cms';
export function Features({ block }: { block: CmsBlock }) { const items = Array.isArray(block.items) ? block.items as RepeaterItem[] : []; return <section className="block muted"><div className="shell"><h2>{String(block.headline || '')}</h2><p className="lead">{String(block.subheadline || '')}</p><div className={`cards cols-${Number(block.columns || 3)}`}>{items.map((item, index) => <article className="card" key={index}><span className="feature-icon">{String(item.icon || '✓')}</span><h3>{String(item.title || '')}</h3><p>{String(item.description || '')}</p>{item.link ? <a href={String(item.link)}>Plačiau →</a> : null}</article>)}</div></div></section>; }
